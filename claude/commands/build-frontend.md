Use the frontend agent for: $ARGUMENTS

Requirements:
- inspect existing UI and routing first
- keep components modular
- preserve enterprise-grade UX
- reuse existing patterns where sensible
- avoid fake metrics, filler copy, and toy styling
- summarize UX tradeoffs and next improvements